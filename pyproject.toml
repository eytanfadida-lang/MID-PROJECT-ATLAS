[tool.pytest.ini_options]
# הפרויקט הוא עדיין מודולים שטוחים בשורש (לא חבילה) - צריך להבטיח שהשורש
# תמיד נמצא ב-sys.path בזמן איסוף הבדיקות, בלי קשר לאיך ואיפה pytest מופעל
pythonpath = ["."]
testpaths = ["tests"]
